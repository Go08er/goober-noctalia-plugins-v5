id = "goober/nocvox"
name = "NocVox"
version = "0.3.0"
plugin_api = 17
author = "goober"
license = "MIT"
icon = "microphone"
description = "A live status and recording-control companion for an existing VoxType daemon."
deprecated = false
tags = ["audio", "bar", "indicator", "privacy", "productivity", "recording", "service", "utility"]
dependencies = ["voxtype"]

# The service is the only owner of the long-lived VoxType status follower and
# the only entry allowed to launch VoxType recording-control commands.
[[service]]
id = "listener"
entry = "service.luau"

[[widget]]
id = "nocvox"
entry = "widget.luau"

  # Use Noctalia's native per-placement Actions editor. Middle is deliberately
  # omitted so the shell's standard "open this widget's settings" action wins.
  # Recording commands target the singleton service directly, so two widget
  # placements can never fan one click into duplicate VoxType commands.
  [widget.actions]
  left = "plugin goober/nocvox:listener all toggle"
  right = "panel-toggle goober/nocvox:details"

  # Every declared setting below changes only this bar placement. NocVox's
  # singleton listener has no user-configurable policy, so there is no second
  # plugin-wide settings page containing presentation controls.
  [[widget.setting]]
  key = "tooltip_show_model"
  type = "bool"
  label_key = "settings.tooltip_show_model.label"
  default = true

  [[widget.setting]]
  key = "tooltip_show_device"
  type = "bool"
  label_key = "settings.tooltip_show_device.label"
  default = true

  [[widget.setting]]
  key = "tooltip_show_backend"
  type = "bool"
  label_key = "settings.tooltip_show_backend.label"
  default = true

  [[widget.setting]]
  key = "idle_color"
  type = "color"
  label_key = "settings.colors.idle"
  default = "on_surface"

  [[widget.setting]]
  key = "recording_color"
  type = "color"
  label_key = "settings.colors.recording"
  default = "error"

  [[widget.setting]]
  key = "transcribing_color"
  type = "color"
  label_key = "settings.colors.transcribing"
  default = "tertiary"

  [[widget.setting]]
  key = "stopped_color"
  type = "color"
  label_key = "settings.colors.stopped"
  default = "on_surface_variant"

  [[widget.setting]]
  key = "unknown_color"
  type = "color"
  label_key = "settings.colors.unknown"
  default = "tertiary"

  [[widget.setting]]
  key = "idle_display"
  type = "select"
  label_key = "settings.idle_display.label"
  default = "glyph_only"
  options = [
    { value = "glyph_only", label_key = "settings.idle_display.glyph_only" },
    { value = "glyph_text", label_key = "settings.idle_display.glyph_text" },
    { value = "hidden", label_key = "settings.idle_display.hidden" },
  ]

  [[widget.setting]]
  key = "active_label"
  type = "select"
  label_key = "settings.active_label.label"
  description_key = "settings.active_label.description"
  default = "rec"
  options = [
    { value = "rec", label_key = "settings.active_label.rec" },
    { value = "elapsed", label_key = "settings.active_label.elapsed" },
    { value = "glyph_only", label_key = "settings.active_label.glyph_only" },
  ]

  [[widget.setting]]
  key = "width_mode"
  type = "select"
  label_key = "settings.width_mode.label"
  default = "compact"
  options = [
    { value = "compact", label_key = "settings.width_mode.compact" },
    { value = "expanded", label_key = "settings.width_mode.expanded" },
  ]

  # Glyph settings intentionally live at widget scope, where Noctalia v5 shows
  # its native searchable glyph picker.
  [[widget.setting]]
  key = "idle_glyph"
  type = "glyph"
  label_key = "settings.glyphs.idle"
  default = "microphone"

  [[widget.setting]]
  key = "active_glyph"
  type = "glyph"
  label_key = "settings.glyphs.active"
  default = "microphone"

  [[widget.setting]]
  key = "stopped_glyph"
  type = "glyph"
  label_key = "settings.glyphs.stopped"
  default = "microphone-off"

  [[widget.setting]]
  key = "unknown_glyph"
  type = "glyph"
  label_key = "settings.glyphs.unknown"
  default = "microphone"

[[panel]]
id = "details"
entry = "panel.luau"
width = 480
height = 620
placement = "attached"
position = "auto"
open_near_click = true
dismiss_on_outside_click = true
